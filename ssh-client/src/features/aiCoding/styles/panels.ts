import type React from "react";

import { ROW_HEIGHT } from "../components/file-explorer/types";

// 感谢页卡片名称的共用排版，thanksName / thanksNameCopied 仅颜色不同。
// 通用设置开关(toggle switch)的共用基样式。On / Disabled 变体是完整样式对象,
// 组件内用三元在模块样式间切换,不做行内合并(AGENTS.md 样式规范)。
const settingToggleBase: React.CSSProperties = {
  display: "flex",
  alignItems: "center",
  justifyContent: "space-between",
  gap: 14,
  width: "100%",
  padding: "12px 14px",
  borderRadius: 10,
  border: "1px solid var(--border-dim)",
  background: "var(--bg-subtle)",
  cursor: "pointer",
  textAlign: "left" as const,
};

const settingToggleTrackBase: React.CSSProperties = {
  display: "inline-flex",
  alignItems: "center",
  flexShrink: 0,
  width: 40,
  height: 24,
  borderRadius: 999,
  padding: 3,
  boxSizing: "border-box" as const,
  transition: "background 0.12s",
};

const settingToggleKnobBase: React.CSSProperties = {
  display: "block",
  flexShrink: 0,
  width: 18,
  height: 18,
  borderRadius: 999,
  background: "var(--control-knob-bg)",
  transition: "transform 0.12s ease",
};

// 通用设置字段区块(标签 + 控件 + 提示的纵向布局),Spaced 变体带区块间距。
const settingFieldBase: React.CSSProperties = {
  display: "flex",
  flexDirection: "column",
  gap: 5,
};

const thanksNameBase: React.CSSProperties = {
  maxWidth: "100%",
  fontSize: 11.5,
  fontWeight: 600,
  overflow: "hidden",
  textOverflow: "ellipsis",
  whiteSpace: "nowrap",
};

const sendButtonBase: React.CSSProperties = {
  display: "flex",
  alignItems: "center",
  justifyContent: "center",
  gap: 6,
  minWidth: 82,
  minHeight: 36,
  padding: "7px 15px",
  background: "var(--primary-action-bg)",
  color: "var(--primary-action-fg)",
  border: "none",
  borderRadius: 10,
  fontSize: 13,
  fontWeight: 600,
  cursor: "pointer",
  boxShadow: "var(--shadow-control)",
  whiteSpace: "nowrap",
  transition: "opacity 0.15s, background 0.15s",
};

export const panels = {
  newTaskOuter: {
    flex: 1,
    display: "flex",
    flexDirection: "column",
    alignItems: "center",
    justifyContent: "center",
    padding: "0 48px",
    position: "relative",
    overflow: "visible",
  },
  newTaskHeader: {
    marginBottom: 20,
    textAlign: "center",
    display: "flex",
    flexDirection: "column",
    alignItems: "center",
  },
  newTaskClaudeGif: {
    display: "block",
    width: "min(128px, 32vw)",
    height: "auto",
    marginBottom: 10,
    objectFit: "contain",
  },
  newTaskTitle: {
    display: "block",
    fontSize: 22,
    fontWeight: 700,
    color: "var(--text-primary)",
    letterSpacing: -0.3,
    marginBottom: 8,
  },
  newTaskMeta: {
    display: "inline-flex",
    alignItems: "center",
    gap: 6,
    fontSize: 12.5,
    color: "var(--text-muted)",
  },
  metaChip: {
    background: "var(--bg-hover)",
    border: "1px solid var(--border-dim)",
    borderRadius: 4,
    padding: "2px 7px",
    fontSize: 12,
  },
  composeCard: {
    width: "100%",
    maxWidth: 820,
    background: "var(--bg-card)",
    border: "1px solid var(--border-medium)",
    borderRadius: 24,
    boxShadow: "var(--shadow-compose)",
    overflow: "visible",
  },
  composeCardRelative: {
    width: "100%",
    maxWidth: 820,
    background: "var(--bg-card)",
    border: "1px solid var(--border-medium)",
    borderRadius: 24,
    boxShadow: "var(--shadow-compose)",
    overflow: "visible",
    position: "relative" as const,
  },
  agentMissingMdBanner: {
    display: "flex",
    alignItems: "flex-start",
    gap: 10,
    width: "100%",
    maxWidth: 700,
    marginBottom: 10,
    padding: "10px 14px",
    background: "var(--warning-surface)",
    border: "1px solid var(--warning-border)",
    borderRadius: 8,
    boxSizing: "border-box" as const,
  },
  agentMissingMdBody: {
    display: "flex",
    flexDirection: "column" as const,
    gap: 8,
    minWidth: 0,
    flex: 1,
  },
  agentMissingMdIcon: {
    color: "var(--warning)",
    flexShrink: 0,
    marginTop: 1,
  },
  agentMissingMdText: {
    fontSize: 13,
    lineHeight: 1.55,
    color: "var(--text-secondary)",
  },
  agentMissingMdTitle: { fontWeight: 650, color: "var(--text-primary)" },
  agentMissingMdCode: {
    fontFamily: "var(--font-mono)",
    fontSize: 12,
    background: "var(--warning-code-bg)",
    padding: "0 4px",
    borderRadius: 3,
  },
  agentMissingMdInitBtn: {
    alignSelf: "flex-start",
    display: "inline-flex",
    alignItems: "center",
    gap: 6,
    padding: "4px 10px",
    background: "transparent",
    color: "var(--warning)",
    border: "1px solid var(--warning-border)",
    borderRadius: 6,
    fontSize: 12.5,
    fontWeight: 600,
    cursor: "pointer",
    transition: "background 120ms ease",
  },
  agentMissingMdInitBtnHovered: {
    alignSelf: "flex-start",
    display: "inline-flex",
    alignItems: "center",
    gap: 6,
    padding: "4px 10px",
    background: "var(--warning-surface)",
    color: "var(--warning)",
    border: "1px solid var(--warning-border)",
    borderRadius: 6,
    fontSize: 12.5,
    fontWeight: 600,
    cursor: "pointer",
    transition: "background 120ms ease",
  },
  hookFallbackIcon: {
    color: "var(--warning)",
    flexShrink: 0,
    marginTop: 1,
  },
  hookFallbackText: {
    fontSize: 13,
    lineHeight: 1.55,
    color: "var(--text-secondary)",
  },
  // 项目栏状态指示：小圆点(默认)与待确认数量角标
  railStatusDot: {
    position: "absolute" as const,
    bottom: -1,
    right: -1,
    width: 9,
    height: 9,
    borderRadius: "50%",
    borderStyle: "solid" as const,
    borderWidth: 2,
    boxSizing: "border-box" as const,
    // 必须高于 railAvatarStacked(zIndex:1),否则被抬层的头像盖住
    zIndex: 2,
  },
  // 招手小人:绝对定位在头像右下,z-index 低于头像,缩回时被头像遮住
  railMascot: {
    position: "absolute" as const,
    left: 14,
    bottom: -3,
    height: 40,
    width: "auto" as const,
    zIndex: 0,
    pointerEvents: "none" as const,
  },
  // 头像抬到小人之上,使探头/缩回有被遮挡的层次
  railAvatarStacked: {
    position: "relative" as const,
    zIndex: 1,
  },
  railAttentionBadge: {
    position: "absolute" as const,
    top: -5,
    right: -5,
    minWidth: 16,
    height: 16,
    padding: "0 4px",
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
    borderRadius: 999,
    background: "var(--color-warning)",
    color: "#fff",
    fontSize: 10,
    fontWeight: 700,
    lineHeight: 1,
    borderStyle: "solid" as const,
    borderWidth: 2,
    boxSizing: "border-box" as const,
    pointerEvents: "none" as const,
    // 必须高于 railAvatarStacked(zIndex:1),否则被抬层的头像盖住
    zIndex: 2,
  },
  // 通用设置：字段区块(标签 + 控件 + 提示)
  settingField: settingFieldBase,
  settingFieldSpaced: {
    ...settingFieldBase,
    marginTop: 18,
  },
  settingFieldLabel: {
    fontSize: 12,
    fontWeight: 600,
    color: "var(--text-secondary)",
    marginBottom: 5,
    display: "block",
  },
  settingFieldHint: {
    fontSize: 11,
    color: "var(--text-hint)",
    marginTop: 3,
  },
  // 通用设置：开关(toggle switch)
  settingToggle: settingToggleBase,
  // 平台不适用(如 macOS 上的 Windows 专属项)或加载中:置灰且不可点
  settingToggleDisabled: {
    ...settingToggleBase,
    cursor: "default",
    opacity: 0.55,
  },
  settingToggleLabel: {
    fontSize: 13,
    fontWeight: 600,
    color: "var(--text-primary)",
  },
  settingToggleTrack: {
    ...settingToggleTrackBase,
    background: "var(--border-medium)",
  },
  settingToggleTrackOn: {
    ...settingToggleTrackBase,
    background: "var(--primary-action-bg)",
  },
  settingToggleKnob: settingToggleKnobBase,
  settingToggleKnobOn: {
    ...settingToggleKnobBase,
    transform: "translateX(16px)",
  },
  composeTextarea: {
    display: "block",
    width: "100%",
    padding: "22px 24px 12px",
    border: "none",
    outline: "none",
    resize: "none",
    background: "transparent",
    color: "var(--text-primary)",
    fontSize: 14,
    lineHeight: 1.65,
    fontFamily: "var(--font-ui)",
  },
  toolbar: {
    borderTop: "none",
    minHeight: 58,
    padding: "8px 18px 16px",
    display: "flex",
    alignItems: "center",
    gap: 6,
    background: "transparent",
    borderRadius: "0 0 24px 24px",
    boxSizing: "border-box" as const,
  },
  toolbarLeft: {
    display: "flex",
    alignItems: "center",
    gap: 4,
    minWidth: 0,
    flexWrap: "nowrap" as const,
  },
  taskModelCompactTrigger: {
    minWidth: 0,
    maxWidth: 280,
    minHeight: 32,
    display: "inline-flex",
    alignItems: "center",
    gap: 5,
    padding: "5px 10px",
    background: "transparent",
    border: "1px solid transparent",
    borderRadius: 999,
    color: "var(--text-primary)",
    fontSize: 13,
    fontWeight: 500,
    fontFamily: "var(--font-ui)",
    cursor: "pointer",
    outline: "none",
    flexShrink: 1,
    whiteSpace: "nowrap" as const,
    transition: "background 0.15s, color 0.15s",
  },
  taskModelCompactTriggerOpen: {
    minWidth: 0,
    maxWidth: 280,
    minHeight: 32,
    display: "inline-flex",
    alignItems: "center",
    gap: 5,
    padding: "5px 10px",
    background: "var(--bg-hover)",
    border: "1px solid transparent",
    borderRadius: 999,
    color: "var(--text-primary)",
    fontSize: 13,
    fontWeight: 500,
    fontFamily: "var(--font-ui)",
    cursor: "pointer",
    outline: "none",
    flexShrink: 1,
    whiteSpace: "nowrap" as const,
    transition: "background 0.15s, color 0.15s",
  },
  taskModelCompactIcon: {
    flexShrink: 0,
  },
  taskModelCompactName: {
    minWidth: 0,
    overflow: "hidden",
    textOverflow: "ellipsis",
    whiteSpace: "nowrap" as const,
  },
  taskModelCompactEffort: {
    flexShrink: 0,
    color: "var(--text-muted)",
  },
  taskModelCompactChevron: {
    flexShrink: 0,
    color: "var(--text-hint)",
  },
  taskModelPopoverContent: {
    zIndex: 9999,
    width: 240,
    maxWidth: "calc(100vw - 32px)",
    maxHeight: 320,
    overflowY: "auto" as const,
    padding: 5,
    background: "var(--bg-card)",
    border: "1px solid var(--border-medium)",
    borderRadius: 9,
    boxShadow: "var(--shadow-md)",
  },
  taskModelSettingsList: {
    display: "flex",
    flexDirection: "column" as const,
    gap: 1,
  },
  taskModelSettingsRow: {
    minHeight: 36,
    justifyContent: "space-between",
    padding: "6px 7px",
    borderRadius: 6,
  },
  taskModelSettingsRowDisabled: {
    minHeight: 36,
    justifyContent: "space-between",
    padding: "6px 7px",
    borderRadius: 6,
    background: "transparent",
    cursor: "default",
    opacity: 0.5,
  },
  taskModelSettingsLabel: {
    color: "var(--text-primary)",
    fontSize: 13,
    fontWeight: 600,
  },
  taskModelSettingsTrailing: {
    display: "flex",
    alignItems: "center",
    justifyContent: "flex-end",
    minWidth: 0,
    gap: 6,
    color: "var(--text-hint)",
  },
  taskModelSettingsValue: {
    minWidth: 0,
    maxWidth: 126,
    overflow: "hidden",
    textOverflow: "ellipsis",
    whiteSpace: "nowrap" as const,
    color: "var(--text-muted)",
    fontSize: 13,
    fontWeight: 500,
  },
  taskModelSubmenuHeader: {
    minHeight: 32,
    padding: "5px 6px",
    color: "var(--text-primary)",
    fontSize: 12.5,
    fontWeight: 600,
  },
  taskModelPopoverList: {
    display: "flex",
    flexDirection: "column" as const,
    gap: 2,
  },
  taskModelPopoverItem: {
    justifyContent: "space-between",
    alignItems: "center",
    minHeight: 32,
    padding: "4px 7px",
  },
  taskModelPopoverItemSelected: {
    justifyContent: "space-between",
    alignItems: "center",
    minHeight: 32,
    padding: "4px 7px",
    background: "var(--bg-hover)",
    color: "var(--text-primary)",
    fontWeight: 600,
  },
  taskModelPopoverItemBody: {
    display: "flex",
    flexDirection: "column" as const,
    alignItems: "flex-start",
    minWidth: 0,
    gap: 2,
  },
  taskModelPopoverItemName: {
    minWidth: 0,
    maxWidth: 174,
    overflow: "hidden",
    textOverflow: "ellipsis",
    whiteSpace: "nowrap" as const,
  },
  taskModelPopoverItemMeta: {
    minWidth: 0,
    maxWidth: 174,
    overflow: "hidden",
    textOverflow: "ellipsis",
    whiteSpace: "nowrap" as const,
    color: "var(--text-hint)",
    fontSize: 10.5,
  },
  taskModelPopoverSeparator: {
    height: 1,
    margin: "4px 3px",
    background: "var(--border-dim)",
  },
  launchModeBar: {
    display: "flex",
    alignItems: "center",
    gap: 4,
    width: "100%",
    maxWidth: 820,
    marginTop: 8,
    padding: "0 18px",
    boxSizing: "border-box" as const,
  },
  toolbarSpacer: {
    flex: 1,
    minWidth: 8,
  },
  toolbarModelSlot: {
    display: "flex",
    justifyContent: "flex-end",
    minWidth: 0,
    maxWidth: 280,
    flexShrink: 1,
  },
  toolbarPlusBtn: {
    width: 26,
    height: 26,
    padding: 0,
    border: "none",
    borderRadius: 6,
    background: "transparent",
    color: "var(--text-muted)",
    display: "inline-flex",
    alignItems: "center",
    justifyContent: "center",
    cursor: "pointer",
    flexShrink: 0,
  },
  toolbarIconBtn: {
    background: "none",
    border: "none",
    cursor: "pointer",
    padding: "5px 7px",
    borderRadius: 5,
    display: "flex",
    alignItems: "center",
  },
  toolbarDivider: {
    width: 1,
    height: 18,
    background: "var(--border-dim)",
    margin: "0 2px",
    flexShrink: 0,
  },
  toolbarBtn: {
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
    gap: 5,
    minHeight: 32,
    padding: "5px 8px",
    background: "transparent",
    border: "1px solid transparent",
    borderRadius: 6,
    fontSize: 13,
    fontWeight: 500,
    color: "var(--text-muted)",
    cursor: "pointer",
    whiteSpace: "nowrap" as const,
    transition: "background 0.15s, border-color 0.15s, opacity 0.15s",
  },
  toolbarBtnActive: {
    background: "transparent",
    border: "1px solid transparent",
    color: "var(--accent)",
  },
  toolbarMenuContent: {
    background: "var(--bg-card)",
    border: "1px solid var(--border-medium)",
    borderRadius: 8,
    boxShadow: "var(--shadow-md)",
    padding: 4,
    minWidth: 180,
    zIndex: 9999,
  },
  toolbarActionMenuContent: {
    background: "var(--bg-card)",
    border: "1px solid var(--border-medium)",
    borderRadius: 8,
    boxShadow: "var(--shadow-md)",
    padding: 6,
    minWidth: 230,
    zIndex: 9999,
  },
  toolbarMenuItem: {
    display: "flex",
    alignItems: "center",
    gap: 8,
    padding: "8px 10px",
    borderRadius: 6,
    fontSize: 13,
    color: "var(--text-primary)",
    cursor: "pointer",
    outline: "none",
    userSelect: "none" as const,
  },
  toolbarHiddenInput: {
    display: "none",
  },
  toolbarMenuButton: {
    width: "100%",
    gap: 8,
  },
  toolbarMenuSwitchItem: {
    width: "100%",
    justifyContent: "space-between",
  },
  toolbarMenuItemLead: {
    display: "inline-flex",
    alignItems: "center",
    gap: 8,
  },
  toolbarMenuSeparator: {
    height: 1,
    background: "var(--border-dim)",
    margin: "6px 6px",
  },
  toolbarMenuItemIcon: {
    width: 14,
    height: 14,
    flexShrink: 0,
  },
  toolbarMenuItemIconMuted: {
    width: 14,
    height: 14,
    flexShrink: 0,
    opacity: 0.72,
  },
  toolbarChevron: {
    opacity: 0.58,
  },
  toolbarSwitchTrack: {
    width: 36,
    height: 20,
    borderRadius: 999,
    padding: 2,
    background: "var(--border-medium)",
    display: "inline-flex",
    alignItems: "center",
    boxSizing: "border-box" as const,
    transition: "background 0.15s",
  },
  toolbarSwitchTrackOn: {
    width: 36,
    height: 20,
    borderRadius: 999,
    padding: 2,
    background: "var(--primary-action-bg)",
    display: "inline-flex",
    alignItems: "center",
    boxSizing: "border-box" as const,
    transition: "background 0.15s",
  },
  toolbarSwitchThumb: {
    width: 16,
    height: 16,
    borderRadius: 999,
    background: "var(--bg-card)",
    boxShadow: "var(--shadow-switch-thumb)",
    transition: "transform 0.15s",
  },
  toolbarSwitchThumbOn: {
    width: 16,
    height: 16,
    borderRadius: 999,
    background: "var(--bg-card)",
    boxShadow: "var(--shadow-switch-thumb)",
    transition: "transform 0.15s",
    transform: "translateX(16px)",
  },
  sendSplit: {
    display: "inline-flex",
    minHeight: 32,
    flexShrink: 0,
  },
  saveTodoBtn: {
    display: "inline-flex",
    alignItems: "center",
    gap: 5,
    padding: "6px 12px",
    background: "var(--bg-hover)",
    color: "var(--text-muted)",
    border: "1px solid var(--border-dim)",
    borderRadius: 6,
    fontSize: 13,
    fontWeight: 500,
    cursor: "pointer",
    transition: "opacity 0.15s",
  },
  sendBtn: sendButtonBase,
  sendBtnPrimary: {
    ...sendButtonBase,
    borderRadius: "6px 0 0 6px",
    borderRight:
      "1px solid color-mix(in srgb, var(--primary-action-fg) 18%, transparent)",
  },
  sendBtnMenu: {
    ...sendButtonBase,
    minWidth: 22,
    minHeight: 32,
    padding: "6px 5px",
    borderRadius: "0 6px 6px 0",
    borderLeft: "none",
  },
  toolbarTodoMenuItem: {
    width: "100%",
    gap: 8,
    cursor: "pointer",
  },
  toolbarTodoMenuItemDisabled: {
    width: "100%",
    gap: 8,
    cursor: "not-allowed",
    opacity: 0.4,
  },
  sendShortcutIcon: {
    display: "inline-flex",
    alignItems: "center",
    justifyContent: "center",
    gap: 3,
    minWidth: 18,
    color: "currentColor",
    opacity: 0.95,
  },
  sendShortcutTextKey: {
    display: "inline-flex",
    alignItems: "center",
    justifyContent: "center",
    minWidth: 16,
    height: 14,
    fontSize: 10,
    fontWeight: 700,
    lineHeight: 1,
    fontFamily: "var(--font-ui)",
  },
  kbd: {
    display: "none",
    fontSize: 11,
    opacity: 0,
    background: "transparent",
    borderRadius: 3,
    padding: 0,
    fontFamily: "var(--font-ui)",
  },
  dropdownPanel: {
    position: "absolute",
    top: "calc(100% + 6px)",
    left: 0,
    zIndex: 100,
    background: "var(--bg-card)",
    border: "1px solid var(--border-medium)",
    borderRadius: 8,
    boxShadow: "var(--shadow-md)",
    padding: 4,
    minWidth: 220,
  },
  dropdownOption: {
    display: "flex",
    alignItems: "flex-start",
    gap: 10,
    padding: "8px 10px",
    borderRadius: 6,
    cursor: "pointer",
  },
  fileChipsRow: {
    display: "flex",
    flexWrap: "wrap" as const,
    gap: 6,
    padding: "10px 14px 4px",
    borderBottom: "1px solid var(--border-dim)",
  },
  fileChip: {
    display: "inline-flex",
    alignItems: "center",
    gap: 5,
    padding: "3px 8px 3px 7px",
    background: "var(--accent-subtle)",
    border: "1px solid color-mix(in srgb, var(--accent) 30%, transparent)",
    borderRadius: 6,
    fontSize: 12.5,
    fontWeight: 500,
    color: "var(--accent)",
    maxWidth: 240,
  },
  fileChipName: { overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" as const },
  fileChipRemove: {
    background: "none",
    border: "none",
    cursor: "pointer",
    padding: 0,
    display: "flex",
    alignItems: "center",
    color: "var(--accent)",
    opacity: 0.6,
    marginLeft: 1,
    flexShrink: 0,
  },
  mentionDropdown: {
    position: "absolute" as const,
    bottom: "calc(100% + 6px)",
    left: 0,
    right: 0,
    zIndex: 200,
    background: "var(--bg-card)",
    border: "1px solid var(--border-medium)",
    borderRadius: 10,
    boxShadow: "var(--shadow-lg, var(--shadow-md))",
    padding: 4,
    maxHeight: 260,
    overflowY: "auto" as const,
  },
  mentionOption: {
    display: "flex",
    alignItems: "center",
    gap: 8,
    padding: "7px 10px",
    borderRadius: 6,
    cursor: "pointer",
    minWidth: 0,
  },
  mentionOptionName: { fontSize: 13, fontWeight: 500, color: "var(--text-primary)", flexShrink: 0 },
  mentionOptionDir: {
    fontSize: 11.5,
    color: "var(--text-hint)",
    overflow: "hidden",
    textOverflow: "ellipsis",
    whiteSpace: "nowrap" as const,
    minWidth: 0,
  },
  mentionSeparator: {
    display: "flex",
    alignItems: "center",
    gap: 6,
    padding: "5px 10px 3px",
    fontSize: 11,
    fontWeight: 600,
    color: "var(--text-hint)",
    letterSpacing: 0.4,
    textTransform: "uppercase" as const,
    userSelect: "none" as const,
  },
  mentionProjectItem: {
    display: "flex",
    alignItems: "center",
    gap: 8,
    padding: "7px 10px",
    borderRadius: 6,
    cursor: "pointer",
    minWidth: 0,
  },
  mentionCrossHeader: {
    display: "flex",
    alignItems: "center",
    gap: 6,
    padding: "5px 10px 5px",
    fontSize: 11.5,
    fontWeight: 500,
    color: "var(--text-muted)",
    borderBottom: "1px solid var(--border-dim)",
    marginBottom: 3,
    userSelect: "none" as const,
  },
  fileExplorerRoot: {
    flexShrink: 0,
    background: "var(--bg-sidebar)",
    borderLeft: "1px solid var(--border-dim)",
    display: "flex",
    flexDirection: "column" as const,
    overflow: "hidden",
  },
  fileExplorerHeader: {
    height: 40,
    display: "flex",
    alignItems: "center",
    padding: "0 12px",
    borderBottom: "1px solid var(--border-dim)",
    flexShrink: 0,
  },
  fileExplorerHeaderTitle: {
    fontSize: 11,
    fontWeight: 700,
    color: "var(--text-hint)",
    letterSpacing: 0.7,
    textTransform: "uppercase" as const,
    flex: 1,
  },
  fileExplorerIconButton: {
    background: "none",
    border: "none",
    cursor: "pointer",
    color: "var(--text-hint)",
    padding: 4,
    borderRadius: 4,
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
  },
  fileExplorerIconButtonActive: {
    background: "var(--bg-selected)",
    color: "var(--accent)",
  },
  fileExplorerRootLabel: {
    display: "flex",
    alignItems: "center",
    gap: 6,
    padding: "6px 8px 3px 20px",
    fontSize: 12.5,
    fontWeight: 600,
    color: "var(--text-primary)",
  },
  fileExplorerRootIcon: {
    width: 5,
    height: 14,
    borderRadius: 2,
    background: "var(--icon-folder-root)",
    flexShrink: 0,
    display: "inline-block",
  },
  fileSearchDialogBackdrop: {
    position: "fixed" as const,
    inset: 0,
    zIndex: 1600,
    display: "flex",
    alignItems: "flex-start",
    justifyContent: "center",
    paddingTop: "18vh",
    background: "color-mix(in srgb, var(--bg-root) 38%, transparent)",
  },
  fileSearchDialog: {
    width: "min(620px, calc(100vw - 56px))",
    maxHeight: "min(560px, calc(100vh - 180px))",
    display: "flex",
    flexDirection: "column" as const,
    padding: 10,
    background: "var(--bg-card)",
    border: "1px solid var(--border-medium)",
    borderRadius: 8,
    boxShadow: "var(--shadow-popover)",
    overflow: "hidden",
  },
  fileSearchDialogHeader: {
    display: "flex",
    alignItems: "center",
    justifyContent: "space-between",
    gap: 8,
    padding: "2px 2px 8px",
    flexShrink: 0,
  },
  fileSearchDialogTitle: {
    fontSize: 12,
    fontWeight: 700,
    color: "var(--text-hint)",
    letterSpacing: 0.7,
    textTransform: "uppercase" as const,
  },
  fileSearchBox: {
    display: "flex",
    alignItems: "center",
    gap: 7,
    minHeight: 30,
    padding: "0 5px 0 9px",
    border: "1px solid var(--border-dim)",
    borderRadius: 6,
    background: "var(--bg-card)",
    boxSizing: "border-box" as const,
  },
  fileSearchIcon: {
    flexShrink: 0,
    color: "var(--text-hint)",
  },
  fileSearchInput: {
    flex: 1,
    minWidth: 0,
    height: 26,
    padding: 0,
    border: "none",
    outline: "none",
    background: "transparent",
    color: "var(--text-primary)",
    fontFamily: "var(--font-ui)",
    fontSize: 12.5,
  },
  fileSearchClearBtn: {
    width: 20,
    height: 20,
    padding: 0,
    border: "none",
    borderRadius: 4,
    background: "transparent",
    color: "var(--text-hint)",
    display: "inline-flex",
    alignItems: "center",
    justifyContent: "center",
    cursor: "pointer",
    flexShrink: 0,
  },
  fileSearchTypeTrigger: {
    height: 22,
    maxWidth: 76,
    display: "inline-flex",
    alignItems: "center",
    gap: 3,
    padding: "0 5px 0 7px",
    border: "1px solid transparent",
    borderRadius: 5,
    background: "var(--bg-hover)",
    color: "var(--text-muted)",
    fontFamily: "var(--font-ui)",
    fontSize: 11.5,
    fontWeight: 600,
    cursor: "pointer",
    outline: "none",
    flexShrink: 0,
    overflow: "hidden",
    whiteSpace: "nowrap" as const,
  },
  fileSearchTypeContent: {
    zIndex: 2000,
    background: "var(--bg-sidebar)",
    border: "1px solid var(--border-medium)",
    borderRadius: 8,
    boxShadow: "var(--shadow-md)",
    padding: 4,
    minWidth: 132,
    overflow: "hidden",
  },
  fileSearchTypeItem: {
    height: 26,
    display: "flex",
    alignItems: "center",
    gap: 8,
    padding: "0 28px 0 9px",
    borderRadius: 5,
    cursor: "pointer",
    color: "var(--text-primary)",
    fontSize: 12.5,
    outline: "none",
    position: "relative",
  },
  fileSearchTypeItemSelected: {
    background: "var(--bg-selected)",
  },
  fileSearchResults: {
    flex: 1,
    minHeight: 0,
    overflowY: "auto" as const,
    paddingTop: 7,
  },
  fileSearchEmpty: {
    padding: "16px 8px",
    fontSize: 12,
    color: "var(--text-hint)",
    textAlign: "center" as const,
  },
  fileSearchResult: {
    width: "100%",
    minHeight: 34,
    display: "flex",
    alignItems: "center",
    gap: 7,
    padding: "5px 7px",
    margin: "1px 0",
    border: "none",
    borderRadius: 5,
    background: "transparent",
    color: "var(--text-primary)",
    textAlign: "left" as const,
    cursor: "pointer",
    fontFamily: "var(--font-ui)",
    boxSizing: "border-box" as const,
  },
  fileSearchResultActive: {
    background: "var(--bg-selected)",
  },
  fileSearchResultMain: {
    display: "flex",
    flexDirection: "column" as const,
    minWidth: 0,
    flex: 1,
    gap: 1,
  },
  fileSearchResultName: {
    overflow: "hidden",
    textOverflow: "ellipsis",
    whiteSpace: "nowrap" as const,
    fontSize: 12.5,
    fontWeight: 500,
    color: "var(--text-primary)",
  },
  fileSearchResultDir: {
    overflow: "hidden",
    textOverflow: "ellipsis",
    whiteSpace: "nowrap" as const,
    fontSize: 11,
    color: "var(--text-hint)",
  },
  fileExplorerTreeScroll: {
    flex: 1,
    overflowY: "auto" as const,
    position: "relative" as const,
  },
  fileExplorerEmpty: {
    padding: "16px 12px",
    fontSize: 12,
    color: "var(--text-hint)",
    textAlign: "center" as const,
  },
  fileExplorerVirtualRow: {
    position: "absolute" as const,
    width: "100%",
  },
  fileIconFolder: {
    display: "inline-flex",
    alignItems: "center",
    flexShrink: 0,
  },
  fileIconFile: {
    width: 5,
    height: 14,
    borderRadius: 2,
    flexShrink: 0,
    display: "inline-block",
  },
  fileTreeRow: {
    display: "flex",
    alignItems: "center",
    gap: 5,
    height: ROW_HEIGHT,
    paddingRight: 8,
    cursor: "pointer",
    borderRadius: 4,
    margin: "0 4px",
    boxSizing: "border-box" as const,
    userSelect: "none" as const,
  },
  fileTreeRowDragging: {
    background: "var(--bg-selected)",
    opacity: 0.58,
    cursor: "grabbing",
  },
  fileTreeDragPreview: {
    position: "fixed" as const,
    zIndex: 2200,
    display: "flex",
    alignItems: "center",
    gap: 7,
    maxWidth: 320,
    height: 30,
    padding: "0 10px",
    color: "var(--text-primary)",
    background: "var(--bg-card)",
    border: "1px solid var(--accent)",
    borderRadius: 6,
    boxShadow: "var(--shadow-popover)",
    pointerEvents: "none" as const,
    transform: "translate(12px, 10px)",
    fontFamily: "var(--font-ui)",
    fontSize: 12,
    fontWeight: 600,
    boxSizing: "border-box" as const,
  },
  fileTreeDragPreviewLabel: {
    minWidth: 0,
    overflow: "hidden",
    textOverflow: "ellipsis",
    whiteSpace: "nowrap" as const,
  },
  fileTreeChevron: {
    width: 12,
    flexShrink: 0,
    display: "inline-flex",
    alignItems: "center",
    color: "var(--text-hint)",
  },
  fileTreeChevronSpacer: {
    width: 12,
    flexShrink: 0,
  },
  fileTreeRowLabel: {
    fontSize: 12.5,
    overflow: "hidden",
    textOverflow: "ellipsis",
    whiteSpace: "nowrap" as const,
    flex: 1,
    fontFamily: "var(--font-ui)",
  },
  fileTreeCreateRow: {
    display: "flex",
    alignItems: "center",
    gap: 5,
    height: ROW_HEIGHT,
    paddingRight: 8,
    margin: "0 4px",
    boxSizing: "border-box" as const,
    background: "var(--bg-selected)",
    borderRadius: 4,
  },
  fileTreeCreateInput: {
    flex: 1,
    minWidth: 0,
    height: 18,
    padding: "0 4px",
    fontSize: 12.5,
    fontFamily: "var(--font-ui)",
    color: "var(--text-primary)",
    background: "var(--bg-input, var(--bg-sidebar))",
    border: "1px solid var(--accent)",
    borderRadius: 3,
    outline: "none",
  },
  fileCtxBackdrop: {
    position: "fixed" as const,
    inset: 0,
    zIndex: 999,
  },
  fileCtxMenu: {
    position: "fixed" as const,
    zIndex: 1000,
    background: "var(--bg-sidebar)",
    border: "1px solid var(--border-dim)",
    borderRadius: 6,
    boxShadow: "var(--shadow-popover)",
    minWidth: 148,
    padding: "3px 0",
    fontSize: 12.5,
  },
  fileCtxSeparator: {
    height: 1,
    background: "var(--border-dim)",
    margin: "4px 6px",
  },
  fileCtxMenuItem: {
    display: "block",
    width: "calc(100% - 8px)",
    height: 26,
    padding: "0 10px",
    cursor: "pointer",
    whiteSpace: "nowrap" as const,
    borderRadius: 3,
    margin: "2px 4px",
    transition: "background 0.1s",
    background: "transparent",
    border: "none",
    textAlign: "left" as const,
    fontSize: 12.5,
    fontFamily: "var(--font-ui)",
    lineHeight: "26px",
  },
  attachmentsRow: {
    display: "flex",
    flexWrap: "wrap",
    gap: 6,
    padding: "6px 12px 0",
  },
  imageAttachmentItem: {
    position: "relative",
    flexShrink: 0,
  },
  imageAttachmentImg: {
    width: 64,
    height: 64,
    objectFit: "cover",
    borderRadius: 6,
    display: "block",
    border: "1px solid var(--border)",
  },
  imageAttachmentRemove: {
    position: "absolute",
    top: -5,
    right: -5,
    width: 16,
    height: 16,
    borderRadius: "50%",
    background: "var(--text-muted)",
    border: "none",
    color: "var(--bg)",
    fontSize: 10,
    lineHeight: "16px",
    cursor: "pointer",
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
    padding: 0,
  },
  textAttachmentChip: {
    position: "relative",
    width: 64,
    height: 64,
    borderRadius: 6,
    border: "1px solid var(--border)",
    background: "var(--bg-secondary)",
    display: "flex",
    flexDirection: "column",
    alignItems: "center",
    justifyContent: "center",
    gap: 2,
    flexShrink: 0,
  },
  textAttachmentIcon: {
    color: "var(--text-muted)",
  },
  textAttachmentSize: {
    fontSize: 10,
    color: "var(--text-hint)",
    fontWeight: 500,
  },
  textAttachmentRemove: {
    position: "absolute",
    top: -5,
    right: -5,
    width: 16,
    height: 16,
    borderRadius: "50%",
    background: "var(--text-muted)",
    border: "none",
    color: "var(--bg)",
    fontSize: 10,
    lineHeight: "16px",
    cursor: "pointer",
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
    padding: 0,
  },

  // ── HooksPanel (app settings → Hook 集成) ──────────────────────────────────
  hooksPanelBody: {
    flex: 1,
    overflowY: "auto" as const,
    display: "flex",
    flexDirection: "column" as const,
    gap: 14,
    padding: "20px",
  },
  hooksPanelLabel: {
    fontSize: 12,
    fontWeight: 600,
    color: "var(--text-secondary)",
    marginBottom: 5,
    display: "block",
  },
  hooksPanelHint: { fontSize: 11, color: "var(--text-hint)", marginTop: 3, lineHeight: 1.5 },
  hooksPanelCard: {
    border: "1px solid var(--border-medium)",
    borderRadius: 8,
    padding: "10px 14px",
    background: "var(--bg-card)",
  },
  hooksPanelRow: {
    display: "flex",
    alignItems: "flex-start",
    gap: 8,
    padding: "6px 0",
    fontSize: 12,
    color: "var(--text-primary)",
    lineHeight: 1.5,
  },
  hooksPanelSubRow: {
    display: "flex",
    alignItems: "flex-start",
    gap: 8,
    padding: "0 0 6px",
    fontSize: 12,
    color: "var(--text-secondary)",
    lineHeight: 1.5,
  },
  hooksPanelRowSpacer: { width: 14, flexShrink: 0, display: "inline-block" },
  hooksPanelVersionLow: { color: "var(--warning)" },
  hooksPanelErrorRow: {
    display: "flex",
    alignItems: "flex-start",
    gap: 8,
    padding: "6px 0",
    fontSize: 12,
    color: "var(--danger)",
    lineHeight: 1.5,
  },
  hooksPanelActions: {
    display: "flex",
    gap: 8,
    flexWrap: "wrap" as const,
    paddingTop: 4,
    borderTop: "1px solid var(--border-dim)",
    marginTop: 2,
  },
  hooksPanelPrimaryBtn: {
    display: "inline-flex",
    alignItems: "center",
    gap: 5,
    padding: "5px 11px",
    background: "var(--primary-action-bg)",
    border: "1px solid transparent",
    borderRadius: 7,
    fontSize: 12,
    fontWeight: 600,
    color: "var(--primary-action-fg)",
    cursor: "pointer",
    whiteSpace: "nowrap" as const,
  },
  hooksPanelSecondaryBtn: {
    display: "inline-flex",
    alignItems: "center",
    gap: 5,
    padding: "5px 11px",
    background: "var(--bg-card)",
    border: "1px solid var(--border-medium)",
    borderRadius: 7,
    fontSize: 12,
    fontWeight: 500,
    color: "var(--text-primary)",
    cursor: "pointer",
    whiteSpace: "nowrap" as const,
  },
  hooksPanelDangerBtn: {
    display: "inline-flex",
    alignItems: "center",
    gap: 5,
    padding: "5px 11px",
    background: "var(--danger-surface)",
    border: "1px solid var(--danger-border)",
    borderRadius: 7,
    fontSize: 12,
    fontWeight: 500,
    color: "var(--danger)",
    cursor: "pointer",
    whiteSpace: "nowrap" as const,
  },
  hooksPanelBtnDisabled: { opacity: 0.5, cursor: "not-allowed" as const },

  // 感谢页（ThanksPanel）
  thanksBody: {
    flex: 1,
    overflowY: "auto" as const,
    padding: "18px 20px",
    display: "flex",
    flexDirection: "column" as const,
    gap: 28,
  },
  thanksSection: {
    display: "flex",
    flexDirection: "column" as const,
    gap: 12,
  },
  thanksSectionHeader: {
    display: "flex",
    flexDirection: "column" as const,
    gap: 3,
  },
  thanksSectionTitle: {
    fontSize: 14,
    fontWeight: 700,
    color: "var(--text-primary)",
  },
  thanksSectionDesc: {
    fontSize: 12,
    color: "var(--text-secondary)",
    lineHeight: 1.5,
  },
  thanksGrid: {
    display: "grid",
    gridTemplateColumns: "repeat(auto-fill, minmax(96px, 1fr))",
    gap: 10,
  },
  // 卡片底色 / 描边 / hover 由 App.css 的 .thanks-card 负责，这里只管布局，
  // 避免行内 background 覆盖样式表的 :hover。
  thanksCard: {
    display: "flex",
    flexDirection: "column" as const,
    alignItems: "center",
    gap: 7,
    width: "100%",
    minWidth: 0,
    padding: "11px 8px",
    borderRadius: 10,
    cursor: "pointer",
    font: "inherit",
    color: "inherit",
    appearance: "none" as const,
    textAlign: "center" as const,
  },
  thanksAvatar: {
    width: 38,
    height: 38,
    borderRadius: "50%",
    objectFit: "cover" as const,
    flexShrink: 0,
    background: "var(--bg-card)",
  },
  thanksName: { ...thanksNameBase, color: "var(--text-primary)" },
  thanksNameCopied: { ...thanksNameBase, color: "var(--accent)" },
} satisfies Record<string, React.CSSProperties>;
