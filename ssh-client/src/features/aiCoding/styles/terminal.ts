import type React from "react";

export const terminal = {
  runHeader: {
    padding: "16px 20px 8px",
    display: "flex",
    alignItems: "center",
    gap: 10,
    flexShrink: 0,
  },
  runMetaRow: {
    display: "flex",
    alignItems: "center",
    gap: 6,
    fontSize: 11,
    color: "var(--text-muted)",
    minWidth: 0,
    overflow: "hidden",
  },
  runMetaFixed: {
    flexShrink: 0,
  },
  runMetaBranchInline: {
    display: "inline-flex",
    alignItems: "center",
    gap: 5,
    minWidth: 0,
    overflow: "hidden",
  },
  runMetaBranchText: {
    minWidth: 0,
    overflow: "hidden",
    textOverflow: "ellipsis",
    whiteSpace: "nowrap" as const,
  },
  runMetricsRow: {
    marginTop: 8,
    display: "flex",
    gap: 12,
    flexWrap: "wrap" as const,
  },
  runMetricPill: {
    display: "flex",
    alignItems: "center",
    gap: 5,
    padding: "3px 8px",
    borderRadius: 6,
    background: "var(--bg-input)",
    border: "1px solid var(--border-dim)",
  },
  runMetricPillButton: {
    font: "inherit",
    cursor: "pointer",
  },
  runMetricPillLabel: {
    fontSize: 10,
    color: "var(--text-hint)",
    fontWeight: 600,
    textTransform: "uppercase" as const,
    letterSpacing: 0.4,
  },
  runMetricPillValue: {
    fontSize: 11,
    color: "var(--text-primary)",
    fontWeight: 600,
  },
  runSessionFilePillWrap: {
    position: "relative" as const,
    display: "inline-flex",
    alignItems: "center",
  },
  runSessionStatusDot: {
    display: "inline-block",
    position: "static" as const,
    bottom: "auto",
    right: "auto",
    flexShrink: 0,
  },
  runSessionCopyTooltip: {
    position: "absolute" as const,
    left: "50%",
    bottom: "calc(100% + 6px)",
    transform: "translateX(-50%)",
    zIndex: 20,
    padding: "5px 8px",
    borderRadius: 6,
    background: "var(--bg-card)",
    border: "1px solid var(--border-medium)",
    boxShadow: "0 8px 24px rgba(0, 0, 0, 0.18)",
    color: "var(--text-secondary)",
    fontSize: 11,
    fontWeight: 600,
    lineHeight: 1.4,
    whiteSpace: "nowrap" as const,
    pointerEvents: "none" as const,
  },
  cancelBtn: {
    display: "flex",
    alignItems: "center",
    gap: 5,
    padding: "4px 10px",
    background: "none",
    border: "1px solid var(--border-medium)",
    borderRadius: 5,
    fontSize: 12,
    color: "var(--text-muted)",
    cursor: "pointer",
    flexShrink: 0,
  },
  resumeBtn: {
    display: "flex",
    alignItems: "center",
    gap: 5,
    padding: "4px 10px",
    background: "none",
    border: "1px solid var(--control-active-fg)",
    borderRadius: 5,
    fontSize: 12,
    color: "var(--control-active-fg)",
    cursor: "pointer",
    flexShrink: 0,
  },
  sessionMoreBtn: {
    display: "flex",
    alignItems: "center",
    gap: 5,
    padding: "4px 9px",
    background: "none",
    border: "1px solid var(--border-medium)",
    borderRadius: 5,
    fontSize: 12,
    color: "var(--text-muted)",
    cursor: "pointer",
    flexShrink: 0,
  },
  sessionActionsMenu: {
    minWidth: 232,
  },
  sessionActionsMenuItem: {
    gap: 8,
    minHeight: 32,
  },
  sessionActionsMenuItemBody: {
    minWidth: 0,
    display: "flex",
    flexDirection: "column",
    alignItems: "flex-start",
    gap: 1,
  },
  sessionActionsMenuItemHint: {
    maxWidth: 190,
    color: "var(--text-hint)",
    fontSize: 10.5,
    lineHeight: 1.35,
    whiteSpace: "normal",
  },
  doneBtn: {
    display: "flex",
    alignItems: "center",
    gap: 5,
    padding: "4px 10px",
    background: "none",
    border: "1px solid rgba(46, 160, 67, 0.35)",
    borderRadius: 5,
    fontSize: 12,
    color: "var(--success)",
    cursor: "pointer",
    flexShrink: 0,
  },
  terminalContainer: { flex: 1, overflow: "hidden" as const, padding: "10px 0 10px 10px" },
  interruptedSessionWrap: {
    flex: 1,
    minHeight: 0,
    display: "flex",
    flexDirection: "column" as const,
    overflow: "hidden",
    background: "var(--bg-root)",
  },
  interruptedBanner: {
    flexShrink: 0,
    display: "flex",
    alignItems: "center",
    flexWrap: "wrap" as const,
    gap: 7,
    minHeight: 38,
    padding: "4px 28px",
    borderBottom: "1px solid var(--border-dim)",
    background: "var(--bg-root)",
  },
  interruptedBannerIcon: {
    width: 18,
    height: 18,
    display: "inline-flex",
    alignItems: "center",
    justifyContent: "center",
    color: "var(--warning)",
    flexShrink: 0,
  },
  interruptedBannerBody: {
    minWidth: 0,
    flex: "1 1 180px",
    display: "flex",
    alignItems: "baseline",
    gap: 8,
    flexWrap: "wrap" as const,
  },
  interruptedBannerTitle: {
    fontSize: 12.5,
    fontWeight: 650,
    color: "var(--text-primary)",
    minWidth: 0,
    overflow: "hidden",
    textOverflow: "ellipsis",
    whiteSpace: "nowrap" as const,
  },
  interruptedBannerText: {
    fontSize: 12,
    lineHeight: 1.5,
    color: "var(--text-muted)",
    overflow: "hidden",
    textOverflow: "ellipsis",
    whiteSpace: "nowrap" as const,
  },
  interruptedBannerActions: {
    display: "inline-flex",
    alignItems: "center",
    justifyContent: "flex-end",
    flexWrap: "wrap" as const,
    gap: 5,
    flexShrink: 0,
  },
  interruptedNoSessionPane: {
    flex: 1,
    minHeight: 0,
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
    padding: "24px 32px",
    color: "var(--text-hint)",
    fontSize: 13,
    lineHeight: 1.6,
    textAlign: "center" as const,
  },
  interruptedPrimaryBtn: {
    display: "inline-flex",
    alignItems: "center",
    justifyContent: "center",
    gap: 4,
    padding: "3px 8px",
    borderRadius: 5,
    border: "1px solid var(--border-dim)",
    color: "var(--text-primary)",
    background: "var(--bg-card)",
    fontSize: 12,
    fontWeight: 600,
    cursor: "pointer",
    minHeight: 26,
    whiteSpace: "nowrap" as const,
  },
  interruptedSecondaryBtn: {
    display: "inline-flex",
    alignItems: "center",
    justifyContent: "center",
    gap: 4,
    padding: "3px 8px",
    borderRadius: 5,
    border: "1px solid rgba(46, 160, 67, 0.24)",
    color: "var(--success)",
    background: "rgba(46, 160, 67, 0.055)",
    fontSize: 12,
    fontWeight: 600,
    cursor: "pointer",
    minHeight: 26,
    whiteSpace: "nowrap" as const,
  },
  interruptedDangerBtn: {
    display: "inline-flex",
    alignItems: "center",
    justifyContent: "center",
    gap: 4,
    padding: "3px 8px",
    borderRadius: 5,
    border: "1px solid rgba(248, 81, 73, 0.24)",
    color: "var(--danger)",
    background: "rgba(248, 81, 73, 0.055)",
    fontSize: 12,
    fontWeight: 600,
    cursor: "pointer",
    minHeight: 26,
    whiteSpace: "nowrap" as const,
  },
} satisfies Record<string, React.CSSProperties>;
