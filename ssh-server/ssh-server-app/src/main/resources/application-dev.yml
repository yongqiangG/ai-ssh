server:
  port: 8091
  # 本地开发仅本机访问（Vite 代理与调试都走 localhost）；prod/server 形态由各自配置决定监听面
  address: 127.0.0.1

# 线程池配置
thread:
  pool:
    executor:
      config:
        core-pool-size: 20
        max-pool-size: 50
        keep-alive-time: 5000
        block-queue-size: 5000
        policy: CallerRunsPolicy

# 数据库配置；启动时配置数据库资源信息
spring:
  config:
    # 仅 dev 挂载 agent 装配配置（Q10：prod 不挂，避免 api-key 为空搞死启动）
    import:
      - "optional:classpath:ssh-agent.yml"
  datasource:
    username: root
    password: 123456
    url: jdbc:mysql://127.0.0.1:13306/ai_ssh?useUnicode=true&characterEncoding=utf8&autoReconnect=true&zeroDateTimeBehavior=convertToNull&serverTimezone=Asia/Shanghai&useSSL=true
    driver-class-name: com.mysql.cj.jdbc.Driver
  hikari:
    pool-name: Retail_HikariCP
    minimum-idle: 15 #最小空闲连接数量
    idle-timeout: 180000 #空闲连接存活最大时间，默认600000（10分钟）
    maximum-pool-size: 25 #连接池最大连接数，默认是10
    auto-commit: true  #此属性控制从池返回的连接的默认自动提交行为,默认值：true
    max-lifetime: 1800000 #此属性控制池中连接的最长生命周期，值0表示无限生命周期，默认1800000即30分钟
    connection-timeout: 30000 #数据库连接超时时间,默认30秒，即30000
    connection-test-query: SELECT 1
  type: com.zaxxer.hikari.HikariDataSource

# MyBatis 配置
mybatis:
  mapper-locations: classpath:/mybatis/mapper/*.xml
  config-location: classpath:/mybatis/config/mybatis-config.xml

# 日志
logging:
  level:
    root: info
  config: classpath:logback-spring.xml

# SSH 加密配置；dev 默认密钥（Base64，解码 32 字节），生产环境必须用环境变量覆盖
ssh:
  crypto:
    secret-key: "ZGV2X2RlZmF1bHRfc2VjcmV0X2tleV8zMmJ5dGVzISE="

# AI 对话配置已迁至 ssh-agent.yml（由上方 spring.config.import 挂载，Q10）。
# 旧 ai.api / ai.agent 段已删除，避免与 ssh-agent.yml 的 ai.agent.config 双轨。