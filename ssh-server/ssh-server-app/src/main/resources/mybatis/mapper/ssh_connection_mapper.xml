<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.johnny.infrastructure.dao.ISshConnectionDao">

    <resultMap id="SshConnectionMap" type="com.johnny.infrastructure.dao.po.SshConnectionPO">
        <id column="id" property="id"/>
        <result column="connection_id" property="connectionId"/>
        <result column="name" property="name"/>
        <result column="host" property="host"/>
        <result column="port" property="port"/>
        <result column="username" property="username"/>
        <result column="auth_type" property="authType"/>
        <result column="password" property="password"/>
        <result column="private_key" property="privateKey"/>
        <result column="key_id" property="keyId"/>
        <result column="user_id" property="userId"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
        <result column="deleted" property="deleted"/>
    </resultMap>

    <sql id="columns">
        id, connection_id, name, host, port, username, auth_type, password, private_key, key_id, user_id, created_at, updated_at, deleted
    </sql>

    <insert id="insert" parameterType="com.johnny.infrastructure.dao.po.SshConnectionPO"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO ssh_connection
            (connection_id, name, host, port, username, auth_type, password, private_key, key_id, user_id)
        VALUES
            (#{connectionId}, #{name}, #{host}, #{port}, #{username}, #{authType}, #{password}, #{privateKey}, #{keyId}, #{userId})
    </insert>

    <update id="update" parameterType="com.johnny.infrastructure.dao.po.SshConnectionPO">
        UPDATE ssh_connection
        SET name        = #{name},
            host        = #{host},
            port        = #{port},
            username    = #{username},
            auth_type   = #{authType},
            password    = #{password},
            private_key = #{privateKey},
            key_id      = #{keyId}
        WHERE connection_id = #{connectionId} AND deleted = 0
    </update>

    <update id="softDeleteByConnectionId" parameterType="java.lang.String">
        UPDATE ssh_connection
        SET deleted = 1
        WHERE connection_id = #{connectionId} AND deleted = 0
    </update>

    <select id="queryByConnectionId" parameterType="java.lang.String" resultMap="SshConnectionMap">
        SELECT <include refid="columns"/>
        FROM ssh_connection
        WHERE connection_id = #{connectionId} AND deleted = 0
    </select>

    <select id="queryByUserId" parameterType="java.lang.String" resultMap="SshConnectionMap">
        SELECT <include refid="columns"/>
        FROM ssh_connection
        WHERE user_id = #{userId} AND deleted = 0
        ORDER BY id DESC
    </select>

    <select id="countByKeyId" parameterType="java.lang.String" resultType="int">
        SELECT COUNT(*)
        FROM ssh_connection
        WHERE key_id = #{keyId} AND deleted = 0
    </select>

    <select id="queryLegacyWithEmbeddedKey" resultMap="SshConnectionMap">
        SELECT <include refid="columns"/>
        FROM ssh_connection
        WHERE deleted = 0
          AND private_key IS NOT NULL AND private_key != ''
          AND (key_id IS NULL OR key_id = '')
    </select>

    <update id="migrateToKeyRef">
        UPDATE ssh_connection
        SET key_id      = #{keyId},
            private_key = ''
        WHERE connection_id = #{connectionId} AND deleted = 0
    </update>

</mapper>
